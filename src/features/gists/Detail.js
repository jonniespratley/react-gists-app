import React, { Component } from 'react';

export default class Detail extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="gists-detail">
        Component content: gists/Detail
      </div>
    );
  }
}
